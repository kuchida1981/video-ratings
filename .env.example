POSTGRES_USER=video_ratings
# 注意: POSTGRES_PASSWORD に @, :, /, ? などの特殊文字を含む場合、DATABASE_URL のパースに失敗することがあります。
# 特殊文字を使用する場合は URL エンコード（例: @ → %40）するか、特殊文字を含まないパスワードを使用してください。
POSTGRES_PASSWORD=video_ratings
POSTGRES_DB=video_ratings

SMB_USERNAME=your_samba_username
SMB_PASSWORD=your_samba_password
