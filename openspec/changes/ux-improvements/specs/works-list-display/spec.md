## ADDED Requirements

### Requirement: 作品一覧は全件返す
作品検索APIは件数制限なしで全件を返す。ページングパラメータ（page, page_size）は廃止する。

#### Scenario: 全件取得
- **WHEN** フロントエンドが作品検索APIを呼ぶ
- **THEN** 登録されている全作品が返される（20件上限なし）

---

### Requirement: 作品一覧レスポンスに出演者を含む
作品検索APIのレスポンスには、各作品の出演者リスト（id と name のみ）を含む。

#### Scenario: 出演者ありの作品
- **WHEN** 出演者が登録されている作品が返される
- **THEN** performers フィールドに出演者の id と name のリストが含まれる

#### Scenario: 出演者なしの作品
- **WHEN** 出演者が登録されていない作品が返される
- **THEN** performers フィールドは空リストになる

---

### Requirement: 作品一覧テーブルの列順と内容
作品一覧テーブルは次の列を次の順序で表示する：出演者・作品名・メーカー・シリーズ・スコア・登録日。

#### Scenario: 出演者が複数名いる作品の表示
- **WHEN** 作品一覧テーブルを表示する
- **THEN** 出演者列に出演者名をカンマ区切りで表示する（例: 「田中花子, 山田太郎」）

#### Scenario: 出演者がいない作品の表示
- **WHEN** 出演者が0名の作品が作品一覧テーブルに表示される
- **THEN** 出演者列に「—」を表示する
