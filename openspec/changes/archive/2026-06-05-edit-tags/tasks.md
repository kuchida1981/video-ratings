## 1. フロントエンド: 編集ステート追加

- [x] 1.1 `TagsPage.tsx` に `editingTagId: number | null` ステートを追加する
- [x] 1.2 `editName` / `editScore` の編集用ステートを追加する
- [x] 1.3 編集フォームを開く `openEdit(tag)` ハンドラーを実装する（追加フォームを閉じる処理を含む）
- [x] 1.4 編集をキャンセルする `closeEdit()` ハンドラーを実装する

## 2. フロントエンド: 保存処理

- [x] 2.1 `updateTag(tagId)` ハンドラーを実装する（`api.tags.update` を呼び出してリロード）

## 3. フロントエンド: UI

- [x] 3.1 タグ一覧の各タグに編集ボタン（鉛筆アイコン等）を追加する
- [x] 3.2 `editingTagId === tag.id` のときインライン編集フォーム（名前・スコア入力 + 保存・キャンセルボタン）を表示する
- [x] 3.3 通常表示と編集フォームの切り替え表示を実装する
- [x] 3.4 タグ追加フォームが開いているときに編集フォームを開くと追加フォームが閉じることを確認する
